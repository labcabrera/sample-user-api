apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample-user-api
  annotations:
    kubernetes.io/ingress.className: nginx-ingress
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sample-user-api.local
      secretName: sample-user-api-tls
  rules:
    - host: sample-user-api.local
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: sample-user-api
                port:
                  number: 80

