apiVersion: v1
kind: Service
metadata:
  name: sample-user-api
  labels:
    k8s-app: sample-user-api
spec:
  selector:
    k8s-app: sample-user-api
  type: ClusterIP
  ports:
  - name: web
    port: 80
    targetPort: 8083
    protocol: TCP